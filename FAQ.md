# 常见问题

## 国内网络无法连接VocabProfiler怎么办？

开始检索前运行：

```bash
python3 scripts/check_vocabulary_service.py
```

这个检查只发送内置短文本，不会发送教师的材料。默认地址不可用时，可以通过 `VOCAB_PROFILE_API_URL` 或 `--api-url` 配置兼容的备用服务。

## 没有可用的词汇服务，还能拿到材料吗？

可以。Skill会先征得用户同意，再把已经完成的来源核验、完整原文和教学分析整理成基础材料包。HTML和Word会明确标出“课标词汇覆盖尚未完成”，不会生成估算数据。

```bash
python3 scripts/prepare_basic_delivery.py path/to/work/material-pack.json \
  --output path/to/work/material-pack.basic.json
```

以后服务恢复时，可以从原始工作文件重新运行词汇分析，生成完整材料包。

## 为什么有时不足3篇？

Skill只收录能够公开完整打开、来源清楚且适合教学的材料。如果合格材料不足3篇，它会报告缺口，并请教师决定放宽年份、地域、篇幅、主题细节或文体中的哪一项。

## 能否使用需要登录或付费的文章？

Skill使用无需登录或付费即可完整阅读的页面，并尊重网站的访问控制。只有摘要、预览或截断正文的页面会记录为淘汰候选。

## “公开可读”是否等于可以印刷或发给学生？

不一定。Skill会分别记录网页访问状态和复用许可。教师或所在机构需要根据许可说明和实际使用场景作最终决定。

## 课标词汇覆盖率是不是文章难度？

不是。覆盖率说明文章词汇与高中英语课标词表的匹配情况。它可以帮助教师发现需要关注的词汇，但文章难度还要结合句法、篇章结构、背景知识和教学任务判断。

## 需要安装哪些软件？

Python脚本要求Python 3.10或更高版本，并且只使用标准库。生成Word报告需要Node.js 18或更高版本，以及仓库中锁定的 `docx` 依赖：

```bash
npm ci --ignore-scripts
```

## 最终会生成哪些文件？

教师会收到一个可离线浏览的HTML材料包、一份Word分析报告，以及每篇文章各自独立的纯文本文件。内部校验数据保留在工作目录，不放入教师交付目录。
